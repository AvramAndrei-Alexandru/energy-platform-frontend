export enum AdminRoutes {
    "Users" = "/users",
    "Register" = "/register"
}

export enum RoutingConstants {
    LOGIN = "login",
    USERS = "users",
    REGISTER = "register",
    DEVICES = "devices",
    MEASUREMENTS = "measurements",
    RPC = "rpc"
}